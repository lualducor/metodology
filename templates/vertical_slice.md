# Vertical Slice

Copy to `docs/05_vertical_slice/slice.md`. Replace prompts with evidence; use `n/a: reason` only when justified.

```yaml
ai_assisted: false
ai_session_id: null
human_reviewed: false
reviewer: null
reviewed_at: null
reviewed_revision: null
```

## Out-of-scope

List tempting additions explicitly excluded from this slice.

## Critical path

Name the one end-to-end user journey and the modules/dependencies it crosses.

## Definition of done

List the ACs and release checks that must pass on the candidate revision.

## Manual fallback

Describe how the user completes the task when the feature fails, or justify why no fallback exists.

## Effort estimate

Estimate implementation, verification, review and process hours separately. Set a re-evaluation point.
