# Product Spec

Copy to `docs/02_spec/spec.md`. Replace prompts with evidence; use `n/a: reason` only when justified.

```yaml
ai_assisted: false
ai_session_id: null
human_reviewed: false
reviewer: null
reviewed_at: null
reviewed_revision: null
```

## Requirements

Assign UC/AC IDs, state intended behavior and relevant non-functional constraints. Link the supporting problem evidence.

## Edge cases

List empty, invalid, boundary, duplicate and concurrent inputs that matter for this slice, and expected behavior.

## Failure states

Describe dependency failure, timeout, partial completion and recovery. State what data must remain unchanged.

## Inputs

Specify values, types, validation and trust boundaries.

## Outputs

Specify results, errors, side effects and observable success/failure behavior.
